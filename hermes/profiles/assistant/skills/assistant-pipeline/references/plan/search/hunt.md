# Hunt — decision surface

An exhaustive multi-hop source hunt: an obscure topic, the primary
sources behind a contested claim, a provenance chase ("who said it
first, where"). The searcher owns the frontier/ledger mechanics and
hop strategy; you own what the hunt must establish and when it may
stop.

Searcher unit `hunt` · QA `hunt` · units: one hunt per unit;
card-eligible as `exhaustive-hunt` once every decision below is
settled — cards run `goal_mode: true` + `goal_max_turns`.

## Fix before release

- **The question** — what the source map must establish, one line;
  "everything about X" is not a hunt, it is an unbounded graph.
- **Done criteria** — the observable stop: the primary source
  located, the claim traced to its origin, every named sub-area
  covered to saturation.
- **Scope exclusions** — hops NOT to take (languages, date ranges,
  derivative coverage, adjacent topics); a hunt without exclusions
  does not terminate.
- **Primary-source requirement** — whether secondary coverage
  satisfies the question or only primary/original sources count;
  the source map marks each hit either way.
- **Budget** — `goal_max_turns` for a card, a turn budget for a
  session unit; exhausting the budget with gaps named is a valid
  outcome, grinding past it is not.

## Defaults

- Saturation is the stop: frontiers that stop yielding new leads
  close; the result is a source map + trail notes + named gaps, and
  honest gaps beat padded coverage.
- Contested claims return the conflicting sources side by side,
  each dated and marked primary/secondary — adjudication is the
  researcher's, and the hunt brief should say who consumes the map.
- Session hunts by default; a card only when you would accept the
  result sight unseen against the done criteria.

## Red flags

- Done criteria missing — "find everything" hunts end by fatigue,
  not by contract; a spec-gap finding.
- The hunt is really a verdict ("find out whether X is true") —
  retrieval feeds the researcher's verification; the hunt maps
  sources, it does not decide.
- Analysis smuggled into the map ("and summarize the debate") —
  synthesis is a researcher unit consuming the map as a part.
- A hunt released to fill a sweep's thin coverage — that is a
  narrowed gap-fill sweep, not an escalation to hunt.
