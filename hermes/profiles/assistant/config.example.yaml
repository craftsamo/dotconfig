model:
  default: claude-opus-5
  provider: anthropic
  base_url: https://api.anthropic.com
providers: {}
fallback_providers:
- provider: openai-codex
  model: gpt-5.6-sol
  base_url: https://chatgpt.com/backend-api/codex
- provider: openrouter
  model: xiaomi/mimo-v2.5
  base_url: https://openrouter.ai/api/v1
  api_mode: chat_completions
credential_pool_strategies: {}
toolsets:
- web
- browser
- terminal
- file
- vision
- x_search
- skills
- todo
- memory
- clarify
- delegation
- cronjob
- computer_use
- kanban
max_concurrent_sessions: null
agent:
  system_prompt: |
    ## Operating contract — assistant (messaging front door + kanban orchestrator)

    SOUL.md is authoritative for the persona's voice, pronouns, character texture,
    language, style, phrasing, and emotional temperature. This contract controls
    mechanics only. Never let operational wording flatten the persona into generic
    assistant prose.

    ### Answer shape

    Keep commands copy-paste-ready in fenced blocks with a language tag; never
    paraphrase a command. Keep plain prose for short conversational replies
    (greeting / ack / clarifying / progress) — don't over-structure trivial answers.

    ### Telegram delivery

    These rules apply to interactive Assistant replies. `no_agent` Cron jobs bypass
    the agent and deliver script stdout verbatim; their formatter, content, and Inbox
    routing belong to the script and runtime, not this prompt.

    Telegram keeps ordinary prose on its legacy Markdown path and sends eligible
    tables, task lists, details, and block math through native rich messages. Prefer
    structured Markdown over plain prose whenever content naturally has sections,
    steps, comparisons, status, tabular data, or optional detail; keep only short
    conversational replies plain. Keep syntax simple so legacy fallback stays readable.
    Keep a reply to roughly one phone screen, lead with the answer, and offer detail
    on request.

    Use these Telegram-friendly constructs proactively whenever the content has
    matching structure; never add them as decoration:
    - Headings and bullet / numbered lists — for sections, steps, comparisons,
      and status.
    - Regular quote (`>`) — for short quoted context, not for hiding
      supplementary detail.
    - Pipe tables (Japanese included) — for genuinely tabular data; keep them
      compact so the rare fallback (bold + bullets) stays readable.
    - Task lists (`- [ ]` / `- [x]`) — native checkboxes; right for plan
      progress and multi-task status.
    - `<details><summary>` — collapsible sections for bulky optional content.
      Separate the summary, each body paragraph, and the closing tag with blank
      lines so the gateway preserves the structure; never place math inside one.
    - `~~strike~~` / `||spoiler||` — light persona accents; never hide
      substantive facts in a spoiler.

    ### Sources

    Never paste raw tool, web, or worker output verbatim into an interactive reply —
    rewrite it in the persona's voice (summarize and format, don't dump).

    ### Memory

    When the master asks you to remember something, or a stable preference recurs, save
    it with the memory tool and briefly say what was stored. Never store secrets, .env
    values, or third-party private information.

    ### Work routing

    The `assistant-pipeline` skill owns the whole flow — modes Chat / Plan /
    Execute / Quality Assurance over tiers inline / resident / kanban —
    auto-loaded in Telegram
    DM sessions; load it (skill_view) before non-trivial work anywhere else.
    Baseline until it loads: conversation and quick lookups stay inline; start
    nothing heavy. Heavy work (creation, writing, deep research, code) runs by
    default in a resident specialist session via
    `~/.hermes/profiles/assistant/scripts/resident-session.sh`, always invoked
    with terminal background=true + notify_on_complete, and supervised
    conversationally: feedback and revisions are follow-up turns into the same
    session, and the session is closed once the user accepts. The kanban board is
    a closed catalog: a card must match a `card_units` entry declared in the
    assistant-pipeline execute references (fire-and-forget settled specs,
    cron-originated work, mass-parallel production, `scheduled` time parking) —
    no matching unit means resident, never a card, and composites (a whole
    video, a campaign, a feature) are never one card. Lean cards only, no v4
    manifests/digests/probes/QA cards. Never do heavy work in your own turn and
    never post to a public channel yourself. GitHub bookkeeping (Issues, boards,
    merges) is yours through gh, after the relevant approval.

    ### Grants

    Grants in session briefs and card bodies (Authority / Budget / Publish) never
    exceed what the user already sanctioned — absent a sanction, the tightest
    default (A1 / creator defaults / draft-only). Later expansions are new
    conversation turns or AUTHORITY+ comments, and only ever widen what the user
    approved.

    ### Quality gate

    You are the quality gate. Every specialist deliverable — session reply or
    card completion — is a candidate until you verified the actual artifact per
    the assistant-pipeline quality-assurance references: files at durable paths, vision on
    images/frames, ffprobe on av media, read the prose, spot-check sources.
    Defects go back to the same session as itemized feedback turns. Deliver only
    what you verified, then close the session. User acceptance is approval, not
    QA.

    ### Blocked cards

    A kanban block (needs_input) notification is yours to triage even before the
    assistant-pipeline skill loads: kanban_show the task, read its STATE:/Q<n>:
    comments, answer what the sanctioned plan already covers as DECISION(Q<n>)
    comments — every open Q<n> in the batch, never a partial set — and relay
    out-of-plan questions to the user. A block whose reason opens with REVIEW: or
    APPROVAL: is ALWAYS relayed — never answer it autonomously. After recording
    every decision, resolve through the absolute
    `~/.hermes/profiles/assistant/scripts/kanban-resolve-block.sh apply <id>`;
    never call `kanban_unblock` directly. One answer round per card is the cap:
    a second block, a `capability` block, or a question that reveals a spec gap
    is pulled back (archive, then resident session or re-plan) — never argued
    into running.

    ### Kanban worker mode

    When spawned as a kanban worker (HERMES_KANBAN_TASK is set) you are NOT in a
    chat: no user, no Telegram audience. Follow the task body and the injected
    kanban guidance only — kanban_show the task and its parent cards, do the work
    the body asks for, and end every run with kanban_complete or kanban_block.
    The answer-shape, memory, and work-routing sections above do not apply. Never
    create cards in worker mode; propose follow-up work in your completion
    summary instead. Never widen a task body's grant.
  max_turns: 300
  gateway_timeout: 1800
  restart_drain_timeout: 180
  api_max_retries: 3
  service_tier: ''
  reasoning_effort: medium
  tool_use_enforcement: auto
  task_completion_guidance: true
  environment_probe: true
  environment_hint: ''
  coding_context: auto
  gateway_timeout_warning: 900
  clarify_timeout: 600
  gateway_notify_interval: 180
  gateway_auto_continue_freshness: 3600
  image_input_mode: auto
  disabled_toolsets: []
terminal:
  backend: local
  modal_mode: auto
  cwd: ~/Workspaces
  timeout: 180
  env_passthrough: []
  shell_init_files: []
  auto_source_bashrc: true
  docker_image: nikolaik/python-nodejs:python3.11-nodejs20
  docker_forward_env: []
  docker_env: {}
  singularity_image: docker://nikolaik/python-nodejs:python3.11-nodejs20
  modal_image: nikolaik/python-nodejs:python3.11-nodejs20
  daytona_image: nikolaik/python-nodejs:python3.11-nodejs20
  container_cpu: 1
  container_memory: 5120
  container_disk: 51200
  container_persistent: true
  docker_volumes: []
  docker_mount_cwd_to_workspace: false
  docker_extra_args: []
  docker_run_as_host_user: false
  persistent_shell: true
web:
  backend: ''
  search_backend: ''
  extract_backend: ''
browser:
  inactivity_timeout: 120
  command_timeout: 30
  record_sessions: false
  allow_private_urls: false
  engine: auto
  auto_local_for_private_urls: true
  cdp_url: ''
  dialog_policy: must_respond
  dialog_timeout_s: 300
  camofox:
    managed_persistence: false
    user_id: ''
    session_key: ''
    adopt_existing_tab: false
    rewrite_loopback_urls: false
    loopback_host_alias: host.docker.internal
checkpoints:
  enabled: false
  max_snapshots: 20
  max_total_size_mb: 500
  max_file_size_mb: 10
  auto_prune: true
  retention_days: 7
  delete_orphans: true
  min_interval_hours: 24
file_read_max_chars: 100000
tool_output:
  max_bytes: 50000
  max_lines: 2000
  max_line_length: 2000
tool_loop_guardrails:
  warnings_enabled: true
  hard_stop_enabled: false
  warn_after:
    exact_failure: 2
    same_tool_failure: 3
    idempotent_no_progress: 2
  hard_stop_after:
    exact_failure: 5
    same_tool_failure: 8
    idempotent_no_progress: 5
compression:
  enabled: true
  threshold: 0.5
  target_ratio: 0.2
  protect_last_n: 20
  hygiene_hard_message_limit: 400
  protect_first_n: 3
  abort_on_summary_failure: false
  codex_gpt55_autoraise: true
prompt_caching:
  cache_ttl: 5m
openrouter:
  response_cache: true
  response_cache_ttl: 300
  min_coding_score: 0.65
bedrock:
  region: ''
  discovery:
    enabled: true
    provider_filter: []
    refresh_interval: 3600
  guardrail:
    guardrail_identifier: ''
    guardrail_version: ''
    stream_processing_mode: async
    trace: disabled
auxiliary:
  vision:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 120
    extra_body: {}
    download_timeout: 30
  web_extract:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 360
    extra_body: {}
  compression:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 120
    extra_body: {}
  skills_hub:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 30
    extra_body: {}
  approval:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 30
    extra_body: {}
  mcp:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 30
    extra_body: {}
  title_generation:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 30
    extra_body: {}
  tts_audio_tags:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 30
    extra_body: {}
  triage_specifier:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 120
    extra_body: {}
  kanban_decomposer:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 180
    extra_body: {}
  profile_describer:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 60
    extra_body: {}
  curator:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 600
    extra_body: {}
  monitor:
    provider: auto
    model: ''
    base_url: ''
    api_key: ''
    timeout: 60
    extra_body: {}
display:
  compact: false
  personality: ''
  resume_display: full
  resume_exchanges: 10
  resume_max_user_chars: 300
  resume_max_assistant_chars: 200
  resume_max_assistant_lines: 3
  resume_skip_tool_only: true
  busy_input_mode: interrupt
  interface: cli
  tui_auto_resume_recent: false
  tui_agents_nudge: true
  bell_on_complete: false
  show_reasoning: false
  streaming: false
  timestamps: false
  final_response_markdown: strip
  persistent_output: true
  persistent_output_max_lines: 200
  persist_prompts: true
  inline_diffs: true
  file_mutation_verifier: true
  credits_notices: true
  turn_completion_explainer: true
  show_cost: false
  skin: default
  language: en
  tui_status_indicator: kaomoji
  user_message_preview:
    first_lines: 2
    last_lines: 2
  interim_assistant_messages: true
  tool_progress_command: false
  tool_progress_overrides: {}
  tool_preview_length: 0
  ephemeral_system_ttl: 0
  platforms:
    telegram:
      streaming: false
    discord:
      streaming: true
  runtime_footer:
    enabled: false
    fields:
    - model
    - context_pct
    - cwd
  copy_shortcut: auto
  tool_progress: all
dashboard:
  theme: default
  show_token_analytics: false
  oauth:
    client_id: ''
    portal_url: ''
  basic_auth:
    username: ''
    password_hash: ''
    password: ''
    secret: ''
    session_ttl_seconds: 0
  public_url: ''
privacy:
  redact_pii: false
tts:
  provider: tts-fallback
  fallback:
    chain:
    - qwen3-tts
    - edge
  qwen3_tts:
    base_url: http://127.0.0.1:10102
    synthesis_timeout: 600
  aivis:
    base_url: http://127.0.0.1:10101
    speaker: "888753760"
    synthesis_timeout: 600
  edge:
    voice: ja-JP-NanamiNeural
  elevenlabs:
    voice_id: pNInz6obpgDQGcFmaJgB
    model_id: eleven_multilingual_v2
  openai:
    model: gpt-4o-mini-tts
    voice: alloy
  gemini:
    model: gemini-2.5-flash-preview-tts
    voice: Kore
    audio_tags: false
    persona_prompt_file: ''
  xai:
    voice_id: eve
    language: en
    sample_rate: 24000
    bit_rate: 128000
  mistral:
    model: voxtral-mini-tts-2603
    voice_id: c69964a6-ab8b-4f8a-9465-ec0925096ec8
  neutts:
    ref_audio: ''
    ref_text: ''
    model: neuphonic/neutts-air-q4-gguf
    device: cpu
  piper:
    voice: en_US-lessac-medium
stt:
  enabled: true
  provider: stt-fallback
  fallback:
    chain:
    - groq
    - xai
    - openai
    - elevenlabs
    - local
  local:
    model: medium
    language: ''
  openai:
    model: whisper-1
  mistral:
    model: voxtral-mini-latest
  elevenlabs:
    model_id: scribe_v2
    language_code: ''
    tag_audio_events: false
    diarize: false
voice:
  record_key: ctrl+b
  max_recording_seconds: 120
  auto_tts: true
  beep_enabled: true
  silence_threshold: 200
  silence_duration: 3.0
  tts_streaming: false
human_delay:
  mode: 'off'
  min_ms: 800
  max_ms: 2500
context:
  engine: compressor
memory:
  memory_enabled: true
  user_profile_enabled: true
  write_approval: false
  memory_char_limit: 2200
  user_char_limit: 1375
  provider: ''
delegation:
  model: ''
  provider: ''
  base_url: ''
  api_key: ''
  api_mode: ''
  inherit_mcp_toolsets: true
  max_iterations: 50
  child_timeout_seconds: 0
  reasoning_effort: ''
  max_concurrent_children: 3
  max_spawn_depth: 1
  orchestrator_enabled: true
  subagent_auto_approve: false
prefill_messages_file: ''
goals:
  max_turns: 20
skills:
  external_dirs:
  - ~/ghq/github.com/NousResearch/hermes-agent/skills/apple
  - ~/ghq/github.com/NousResearch/hermes-agent/skills/creative
  - ~/ghq/github.com/NousResearch/hermes-agent/skills/email
  - ~/ghq/github.com/NousResearch/hermes-agent/skills/github
  - ~/ghq/github.com/NousResearch/hermes-agent/skills/media
  - ~/ghq/github.com/NousResearch/hermes-agent/skills/note-taking
  - ~/ghq/github.com/NousResearch/hermes-agent/skills/productivity
  - ~/ghq/github.com/NousResearch/hermes-agent/skills/research
  - ~/ghq/github.com/NousResearch/hermes-agent/skills/smart-home
  - ~/ghq/github.com/NousResearch/hermes-agent/skills/social-media
  - ~/ghq/github.com/NousResearch/hermes-agent/optional-skills/communication/one-three-one-rule
  - ~/ghq/github.com/NousResearch/hermes-agent/optional-skills/devops/watchers
  - ~/.hermes/skills
  disabled:
  - comfyui
  - touchdesigner-mcp
  - manim-video
  - ascii-video
  template_vars: true
  inline_shell: false
  inline_shell_timeout: 10
  guard_agent_created: false
  write_approval: false
curator:
  enabled: true
  interval_hours: 168
  min_idle_hours: 2
  stale_after_days: 30
  archive_after_days: 90
  prune_builtins: true
  backup:
    enabled: true
    keep: 5
honcho: {}
timezone: ''
slack:
  require_mention: true
  free_response_channels: ''
  allowed_channels: ''
  channel_prompts: {}
discord:
  require_mention: true
  free_response_channels: ''
  allowed_channels: ''
  auto_thread: true
  thread_require_mention: false
  history_backfill: true
  history_backfill_limit: 50
  reactions: true
  channel_prompts: {}
  dm_role_auth_guild: ''
  server_actions: ''
  allow_any_attachment: false
  max_attachment_bytes: 33554432
  voice_fx:
    enabled: false
    ambient_enabled: true
    ambient_path: ''
    ambient_gain: 0.18
    duck_gain: 0.06
    speech_gain: 1.0
    ack_enabled: true
    ack_phrases:
    - Let me look into that.
    - One moment.
    - Checking on that now.
    - Give me a sec.
    - On it.
whatsapp: {}
telegram:
  reactions: false
  channel_skill_bindings:
    - id: 'YOUR_TELEGRAM_CHAT_ID'
      skills:
        - assistant-pipeline
  channel_prompts:
    # On first startup Hermes creates the topics below and persists each thread_id.
    # Replace the four *_THREAD_ID keys with those numeric ids, then restart once.
    # Until that restart, Telegram cron delivery fails closed instead of using root DM.
    "YOUR_TELEGRAM_CHAT_ID": |
      Telegram-specific overrides:
      - This chat renders rich Markdown — lean into it: tables, headings, bullet/numbered
        lists, and task lists (`- [ ]`) are encouraged whenever they make the answer clearer
        or easier to scan.
      - Write documents as plain UTF-8 with NO byte-order mark (BOM); a leading BOM
        makes Telegram show the .md as plain text instead of rendering markdown.
      - Deliver the document by putting its bare file path on its own line; the
        gateway sends it as a native file and strips the path from the text.
    "PERSONAL_THREAD_ID": |
      Topic contract — Personal desk:
      - Apply the personal-desk skill and complete recurring personal operations inline.
      - Never dispatch workers from this topic. When execution must leave the Assistant,
        prepare a compact handoff and ask for a new topic.
      - Treat everything under ~/Workspaces/Personal as sensitive; summarize and never
        expose raw financial data, contacts, or PII.
    "PROJECTS_THREAD_ID": |
      Topic contract — Projects desk:
      - Apply the project-desk skill and complete registry, scaffold, docs, and data
        administration inline.
      - Never dispatch workers from this topic. Code changes or other execution work get a
        compact handoff and a new topic.
      - Never commit or push without explicit approval.
    "INBOX_THREAD_ID": |
      Topic contract — system Inbox:
      - This topic receives automated reports, maintenance notices, scheduled-task releases,
        and watchdog alerts.
      - Summarize or explain a notification when asked, but do not start or dispatch execution
        work here. Prepare a handoff and ask for a new topic when action is required.
      - Normal kanban completion notifications belong to the topic that created the work.
    "BRAINSTORM_THREAD_ID": |
      Topic contract — Brainstorm desk:
      - Apply the brainstorm skill and keep ideation inline.
      - Externalize durable decisions only when useful and with the correct workspace privacy
        rules.
      - Never dispatch workers from this topic. When an idea becomes execution, prepare a
        compact handoff and ask for a new topic.
  allowed_chats: ''
platforms:
  telegram:
    extra:
      rich_messages: true
      # Opt back in to native rich messages for CJK text (local fork patch of
      # upstream PR #56155; upstream gates CJK off by default for #47653).
      allow_cjk_rich_messages: true
      dm_topics:
      - chat_id: 'YOUR_TELEGRAM_CHAT_ID'
        topics:
        - name: Personal
          icon_custom_emoji_id: '5312486108309757006'
          icon_color: 16766590
          skill: personal-desk
        - name: Projects
          icon_custom_emoji_id: '5350554349074391003'
          icon_color: 9367192
          skill: project-desk
        - name: Inbox
          icon_custom_emoji_id: '5417915203100613993'
          icon_color: 7322096
        - name: Brainstorm
          icon_custom_emoji_id: '5310039132297242441'
          icon_color: 13338331
          skill: brainstorm
mattermost:
  require_mention: true
  free_response_channels: ''
  allowed_channels: ''
  channel_prompts: {}
matrix:
  require_mention: true
  free_response_rooms: ''
  allowed_rooms: ''
approvals:
  mode: manual
  timeout: 60
  cron_mode: deny
  mcp_reload_confirm: true
  destructive_slash_confirm: true
command_allowlist: []
quick_commands: {}
hooks: {}
hooks_auto_accept: false
personalities: {}
security:
  allow_private_urls: false
  redact_secrets: true
  tirith_enabled: true
  tirith_path: tirith
  tirith_timeout: 5
  tirith_fail_open: true
  website_blocklist:
    enabled: false
    domains: []
    shared_files: []
  acked_advisories: []
  allow_lazy_installs: true
cron:
  wrap_response: false
  max_parallel_jobs: null
kanban:
  dispatch_in_gateway: true
  dispatch_interval_seconds: 15
  failure_limit: 2
  worker_log_rotate_bytes: 2097152
  worker_log_backup_count: 1
  orchestrator_profile: ''
  default_assignee: ''
  max_in_progress_per_profile: null
  auto_decompose: false
  auto_decompose_per_tick: 3
  dispatch_stale_timeout_seconds: 14400
code_execution:
  mode: project
tools:
  tool_search:
    enabled: auto
    threshold_pct: 10
    search_default_limit: 5
    max_search_limit: 20
logging:
  level: INFO
  max_size_mb: 5
  backup_count: 3
model_catalog:
  enabled: true
  url: https://hermes-agent.nousresearch.com/docs/api/model-catalog.json
  ttl_hours: 1
  providers: {}
network:
  force_ipv4: false
gateway:
  strict: false
  media_delivery_allow_dirs: []
  trust_recent_files: true
  trust_recent_files_seconds: 600
streaming:
  enabled: false
  transport: auto
  edit_interval: 0.8
  buffer_threshold: 24
  cursor: " \u2589"
  fresh_final_after_seconds: 60.0
sessions:
  auto_prune: false
  retention_days: 90
  vacuum_after_prune: true
  min_interval_hours: 24
  write_json_snapshots: false
onboarding:
  seen:
    busy_input_prompt: true
    tool_progress_prompt: true
    openclaw_residue_cleanup: true
    profile_build_offered: true
  profile_build: ask
updates:
  pre_update_backup: false
  backup_keep: 5
  non_interactive_local_changes: stash
lsp:
  enabled: true
  wait_mode: document
  wait_timeout: 5.0
  install_strategy: auto
  servers: {}
x_search:
  model: grok-4.20-reasoning
  timeout_seconds: 180
  retries: 2
secrets:
  bitwarden:
    enabled: false
    access_token_env: BWS_ACCESS_TOKEN
    project_id: ''
    cache_ttl_seconds: 300
    override_existing: true
    auto_install: true
    server_url: ''
paste_collapse_threshold: 5
paste_collapse_threshold_fallback: 5
paste_collapse_char_threshold: 2000
_config_version: 29
platform_toolsets:
  cli:
  - web
  - browser
  - terminal
  - file
  - vision
  - x_search
  - skills
  - todo
  - memory
  - clarify
  - delegation
  - cronjob
  - computer_use
  - kanban
  - no_mcp
  telegram:
  - web
  - browser
  - terminal
  - file
  - vision
  - x_search
  - skills
  - todo
  - memory
  - clarify
  - delegation
  - cronjob
  - computer_use
  - kanban
  - no_mcp
  discord: []
image_gen:
  provider: img-xai-codex-fal
video_gen:
  provider: vid-xai-fal
video_analyze:
  provider: openrouter
  model: xiaomi/mimo-v2.5
plugins:
  enabled:
  - aivis
  - qwen3-tts
  - stt-fallback
  - tts-fallback
  - skill-topology
