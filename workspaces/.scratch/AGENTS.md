# .scratch

Fallback throwaway space for unassigned or intentionally cross-group work. Work owned
by one Group belongs in that Group's `.agent/scratch/<job>/`. Never leave the only copy
of anything important here. Move keepers to the owning Group, `../.notes/`, or
`../.deliverables/`; clear only the current job after acceptance.
